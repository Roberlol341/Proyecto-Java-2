# Etapa 1 — Base y detalle de una receta

Se implementó el consumo de la API pública de Forkify, los modelos de datos, el manejo de timeout, la normalización de imágenes HTTP a HTTPS y el detalle de una receta seleccionada por hash.
